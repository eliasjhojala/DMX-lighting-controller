convertColor(originalColor, fromId, toId);

fromId tells what is originalColor
toId tells what will be returnet color be

originalColor is array containing one of the followings
function also returns an array containing one of the followings

Id   Color
1     { r, g, b }
2     { h, s, b }
3     { r, g, b, w }
4     { r, g, b, w, d }
5     { r, g, b, d }